import 'package:flutter_coffee/features/progress/domain/index.dart';

/// Concrete implementation of [OrderRepository].
///
/// Add dependencies only when this feature actually needs external I/O
/// or integration points, then register the binding in your DI setup.
class OrderRepositoryImpl implements OrderRepository {
  const OrderRepositoryImpl();

  // TODO: implement the methods declared in [OrderRepository].
}
