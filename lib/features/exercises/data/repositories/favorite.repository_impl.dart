import 'package:flutter_coffee/features/exercises/domain/index.dart';

/// Concrete implementation of [FavoriteRepository].
///
/// Add dependencies only when this feature actually needs external I/O
/// or integration points, then register the binding in your DI setup.
class FavoriteRepositoryImpl implements FavoriteRepository {
  const FavoriteRepositoryImpl();

  // TODO: implement the methods declared in [FavoriteRepository].
}
