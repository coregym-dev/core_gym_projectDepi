import 'package:flutter_coffee/features/profile/domain/index.dart';

/// Concrete implementation of [ProfileRepository].
///
/// Add dependencies only when this feature actually needs external I/O
/// or integration points, then register the binding in your DI setup.
class ProfileRepositoryImpl implements ProfileRepository {
  const ProfileRepositoryImpl();

  // TODO: implement the methods declared in [ProfileRepository].
}