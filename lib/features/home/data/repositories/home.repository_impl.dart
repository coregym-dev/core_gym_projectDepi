import 'package:flutter_coffee/features/home/domain/index.dart';

/// Concrete implementation of [HomeRepository].
///
/// Add dependencies only when this feature actually needs external I/O
/// or integration points, then register the binding in your DI setup.
class HomeRepositoryImpl implements HomeRepository {
  const HomeRepositoryImpl();

  // TODO: implement the methods declared in [HomeRepository].
}